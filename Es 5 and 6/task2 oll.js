    const mess = () => {
        const name = "ravi";
        const age = 20 ;
        const city = "rajkot";

        return name + " is " + age + " years old " + "city in "+ city ;
    };
    export default mess ;
